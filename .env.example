GEMINI_API_KEY=        # aistudio.google.com → Get API key → free, no credit card
RESEND_API_KEY=        # resend.com → free tier
TWILIO_ACCOUNT_SID=   # twilio.com → free trial (no credit card)
TWILIO_AUTH_TOKEN=     # twilio.com → free trial
TWILIO_FROM_NUMBER=   # twilio.com → your trial phone number
SECRET_KEY=            # python -c "import secrets; print(secrets.token_hex(32))"
JAEGER_ENDPOINT=http://localhost:4317
OMIUM_API_KEY=         # Get this from the Omium sponsor booth — required for +10% bonus tracing
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_KEY=eyJ...  # service_role key — keep secret, used only in backend
BASE_URL=http://localhost:8000   # Change to your ngrok/tunnel URL for demo
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
STAGE2_THRESHOLD=1    # Set to 2 for production; 1 makes demo reliable with one contact click
